import { useState } from 'react'
import { BarChart3, LockKeyhole, Mail, User } from 'lucide-react'
import { api } from './api'
import './auth.css'
export default function AuthPage({onAuthenticated}:{onAuthenticated:(user:any)=>void}){const [mode,setMode]=useState<'login'|'register'>('login'),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{const result=mode==='login'?await api.login(email,password):await api.register(name,email,password);localStorage.setItem('localsignal-token',result.access_token);localStorage.setItem('localsignal-user',JSON.stringify(result.user));onAuthenticated(result.user)}catch(err:any){setError(err.message||'Unable to continue')}finally{setLoading(false)}};return <div className="auth-page"><section className="auth-intro"><div className="brand"><span className="brandmark"><BarChart3/></span><span>LocalSignal<span>AI</span></span></div><h1>Know your market.<br/>Act on what matters.</h1><p>Local competitor, search, reputation and website intelligence for growing small businesses.</p></section><form className="auth-card" onSubmit={submit}><small>{mode==='login'?'WELCOME BACK':'CREATE YOUR WORKSPACE'}</small><h2>{mode==='login'?'Sign in to LocalSignal':'Create your account'}</h2>{mode==='register'&&<label><User/><input required placeholder="Name" value={name} onChange={e=>setName(e.target.value)}/></label>}<label><Mail/><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label><LockKeyhole/><input required minLength={8} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="auth-error">{error}</div>}<button className="primary" disabled={loading}>{loading?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><div className="auth-switch">{mode==='login'?'New to LocalSignal?':'Already registered?'} <button type="button" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Create account':'Sign in'}</button></div></form></div>}
